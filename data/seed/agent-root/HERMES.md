# Hermes Runtime Notes

This file belongs to Bob, the local default Hermes Agent at `HERMES_HOME`.

Control Hub stores Bob in the `agent_root` SQLite singleton and pushes this file to
`HERMES_HOME/HERMES.md` when the operator syncs Bob to disk.
